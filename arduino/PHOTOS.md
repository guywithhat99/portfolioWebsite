# Images still needed

Placeholders left in the deck. Everything else is either drawn or already dropped in.

Replace a placeholder block with:

```html
<img src="content/img/your-file.jpg" alt="..." style="max-height:420px">
```

Filenames: lowercase and hyphenated, no spaces.


## 11-wiring-bmp280.md

- **PHOTO**: The GY-BMP280 board, close up, with the six pin labels on the silkscreen readable.
- **FRITZING, BREADBOARD VIEW**: BMP280 VCC to the Uno 3.3V pin, GND to GND, SCL to A5, SDA to A4. CSB and SDO unconnected.

## 12-chip-id.md

- **SCREENSHOT**: Table 18 from the datasheet, with the id row at 0xD0 highlighted. Crop to a few rows either side so it is readable on a projector.