#pragma once
#include "Pet.h"

// =============================================================================
// SECTION 4 — BUTTON MASH GAME
// =============================================================================
// Declare your function here, define it in game.cpp.
//
// You need one function:
//   int buttonMash(int pin, int durationMs)
//
// It should:
//   - Open a time window of durationMs milliseconds
//   - Count every fresh button press during that window
//   - Return the total press count when the window closes
// =============================================================================

// === YOUR CODE HERE ===
// int buttonMash(int pin, int durationMs);


// =============================================================================
// FALLBACK
// =============================================================================
// int buttonMash(int pin, int durationMs);
