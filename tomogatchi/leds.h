#pragma once

// =============================================================================
// SECTION 3 — LEDs
// =============================================================================
// Declare your functions here, define them in leds.cpp.
//
// You need three things:
//   void setupLeds()              — configure the three LED pins as OUTPUT
//   void setLed(int r, int g, int b) — turn LED channels on (1) or off (0)
//   bool playSimon()              — PROVIDED: Simon Says game (calls your setLed)
// =============================================================================

// === YOUR CODE HERE ===
// void setupLeds();
// void setLed(int r, int g, int b);


// PROVIDED — declare this too so tomogatchi.ino can call it
// bool playSimon();


// =============================================================================
// FALLBACK
// =============================================================================
// void setupLeds();
// void setLed(int r, int g, int b);
// bool playSimon();
